# API base URL (no trailing slash)
# Dev:    http://localhost:8000/api
# Server: /api (uses reverse proxy)
VITE_API_URL=/api

# Vite dev server settings
VITE_HOST=localhost
VITE_PORT=5173
VITE_ALLOWED_HOSTS=localhost
